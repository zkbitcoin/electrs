use bitcoin::Block;
use crate::pivx::types::PivxParsedTx;

/// Parse a PIVX block — phase 1 stub
pub fn parse_pivx_block(_block: &Block) -> Vec<PivxParsedTx> {
    vec![] // real implementation later
}
